// StackView.fbs
include "Button.fbs";

namespace bdui;

enum Axis: byte {
  Horizontal = 0,
  Vertical = 1
}

table StackView {
  axis: Axis;
  children: [Button];  // Assuming StackView has a list of TestButtons
}
